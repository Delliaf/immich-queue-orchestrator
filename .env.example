# Add these values to the existing Immich .env file. Never commit the real .env file.
IMMICH_API_KEY=
# Optional. Leave empty for a trusted home network; set any non-empty value to enable panel login.
ORCHESTRATOR_ADMIN_PASSWORD=
